import React from "react";

const Infos = ({ city }) => {
  const respostas = "asdasd";
  //   const respostas = fetch(/api/get-city-status/city);
  return <div className={"status-container"}>{city}</div>;
};

export default Infos;
